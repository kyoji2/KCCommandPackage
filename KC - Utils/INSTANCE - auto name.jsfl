//(Auto Name Package)//Auto Name Instance//Justin Putney//http://ajarproductions.com//version 1.0.0var dom = fl.getDocumentDOM();if (dom.selection.length > 0){for(i in dom.selection){	path = dom.selection[i].libraryItem.name;	mc_name = path.substr(path.lastIndexOf("/")+1);	if(mc_name.indexOf(" ") != -1) mc_name = replaceSpaces(mc_name);	dom.selection[i].name = mc_name;	} }function replaceSpaces(str){		var arr = str.split("");		for(var i = 0; i < arr.length; i++){			if(arr[i] == " ") arr[i] = "_";		}		var rStr = arr.join("");		return rStr;}